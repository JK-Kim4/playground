/**
 * 이 로봇은 m x n grid(격자) 위에 있습니다. 로봇은 처음에 좌측 상단 모서리 (grid[0][0])에 위치해 있습니다.
 * 로봇은 우측 하단 모서리 ( grid[m-1][n-1] )로 이동하려고 합니다. 로봇은 한 번에 오른쪽이나 아래쪽으로만 움직일 수 있습니다.
 *
 * 두 정수 m 과 n이 주어졌을 떄, 로봇이 우측 하단 모서리에 도달할 수 있는 가능한 unique paths의 수를 반환하시오
 *
 *
 * example
 * input m = 3, n =7
 * output 28
 * */
public class UniquePaths {

    private int m = 0; // 행
    private int n = 0; // 열

    public UniquePaths(int row, int column) {
        this.m = row;
        this.n = column;
    }

    public int getUniquePathsCount(){

    }
}
